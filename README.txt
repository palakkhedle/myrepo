1.  create github account
2.  create new repo.
3.  create folder in local system
4.  can open in github bash and then run command git init to initialize it
5.  add another file(README) in the folder
6.  then add and commit it.
7.  run this 
    git config --global user.name "palakkhedle"
    git config --global user.email "palakkhedle@gmail.com"
8.  git remote add origin https://github.com/palakkhedle/myrepo.git
9.  git push -u origin master
